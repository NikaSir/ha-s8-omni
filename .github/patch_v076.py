from pathlib import Path
import json
import re

JS = Path("custom_components/s8_omni/frontend/s8-omni-panel.js")
INIT = Path("custom_components/s8_omni/__init__.py")
CONST = Path("custom_components/s8_omni/const.py")
MANIFEST = Path("custom_components/s8_omni/manifest.json")
PANEL = Path("panel.json")
CHANGELOG = Path("CHANGELOG.md")

text = JS.read_text(encoding="utf-8")
text = text.replace('const UI_VERSION = "v0.7.5";', 'const UI_VERSION = "v0.7.6";', 1)

text, count = re.subn(
    r'function productArtData\(mode\) \{.*?\n\}',
    '''function productArtUrl(mode) {\n  const safeMode = mode === "dock" ? "dock" : "clean";\n  return `/s8_omni/product-art/${safeMode}.jpg?v=${encodeURIComponent(UI_VERSION)}`;\n}''',
    text,
    count=1,
    flags=re.S,
)
if count != 1:
    raise SystemExit("productArtData replacement failed")

text = text.replace(
    'if (["charging", "charged"].includes(snap.robot)) return "На базе";\n    return this._label(MODE_LABELS, snap.mode, "Нет данных");',
    'return this._label(MODE_LABELS, snap.mode, "Нет данных");',
    1,
)

text, count = re.subn(
    r'  _heroHint\(snap\) \{.*?\n  \}\n\n  _hero\(\)',
    '''  _heroHint(snap) {\n    if (snap.connection === "disconnected") return "Нет актуальной локальной телеметрии";\n    if (snap.connection === "unknown") return "Текущая связь с устройством не подтверждена";\n    const ops = new Set(snap.stationOperations || []);\n    if (ops.has("drying") || snap.station === "drying") return "Робот на станции и сушится";\n    if (ops.has("roller_cleaning") || snap.station === "roller_cleaning") return "Станция промывает моющий модуль";\n    if (ops.has("dust_collection") || snap.station === "dust_collection") return "Станция выполняет сбор пыли";\n    if (snap.robot === "charged") return "Робот на станции, заряд завершён";\n    if (snap.robot === "charging") return "Робот на станции и заряжается";\n    if (["cleaning", "zone_cleaning", "room_cleaning"].includes(snap.composite)) return "Выполняется уборка";\n    if (snap.composite === "returning_to_dock") return "Робот возвращается на станцию";\n    return "Робот и станция работают как единая система";\n  }\n\n  _hero()''',
    text,
    count=1,
    flags=re.S,
)
if count != 1:
    raise SystemExit("hero hint replacement failed")

hero_start = text.index('  _hero() {')
hero_end = text.index('\n  _quickActions() {', hero_start)
new_hero = '''  _hero() {\n    const snap = this._snapshot();\n    const compositeLabel = snap.connection === "disconnected" ? "Нет связи" : snap.connection === "unknown" ? "Связь не подтверждена" : this._label(COMPOSITE_LABELS, snap.composite, "Нет данных");\n    const connection = this._connectionLabel();\n    const ops = new Set(snap.stationOperations || []);\n    const wash = !snap.unreliable && (ops.has("roller_cleaning") || snap.station === "roller_cleaning");\n    const dust = !snap.unreliable && (ops.has("dust_collection") || snap.station === "dust_collection");\n    const dry = !snap.unreliable && (ops.has("drying") || snap.station === "drying");\n    const charging = !snap.unreliable && snap.robot === "charging";\n    const charged = !snap.unreliable && snap.robot === "charged";\n    const docked = !snap.unreliable && snap.onDock === true;\n    const chargeActive = charging || charged || docked;\n    const artMode = chargeActive || wash || dust || dry ? "dock" : "clean";\n    const battery = snap.battery === null ? "—" : `${Math.round(snap.battery)}%`;\n    const age = snap.age === null ? "—" : this._formatDuration(snap.age);\n    const cleanWaterState = snap.unreliable ? "Нет данных" : "Не контрол.";\n    const dirtyWaterState = snap.unreliable ? "Нет данных" : "Не контрол.";\n    const dustState = snap.unreliable ? "Нет данных" : dust ? "Работает" : "Ожидание";\n    const dryState = snap.unreliable ? "Нет данных" : dry ? "Вкл." : "Выкл.";\n    const chargeState = snap.unreliable ? "Нет данных" : charging ? "Идёт" : charged ? "Завершён" : docked ? "На базе" : "Нет";\n    const mode = this._modeLabel(snap);\n\n    return `<section class="card hero" data-more="composite_status">\n      <div class="hero-top"><div><span class="eyebrow">Состояние</span><h1>${escapeHtml(compositeLabel)}</h1><p class="hero-hint">${escapeHtml(this._heroHint(snap))}</p></div><div class="connection-badge ${connection !== "Локально" ? "bad" : ""}"><i class="dot"></i>${escapeHtml(connection)}</div></div>\n      <div class="omni-scene">\n        <div class="omni-art ${snap.unreliable ? "muted" : ""}">\n          <img class="product-art" src="${productArtUrl(artMode)}" alt="S8 OMNI robot and station" />\n          ${snap.connected ? '<div class="scene-local"><ha-icon icon="mdi:wifi"></ha-icon><span>Локально</span></div>' : ""}\n        </div>\n        <div class="omni-legend">\n          <div class="legend-row clean-water"><ha-icon icon="mdi:water"></ha-icon><span class="legend-copy"><strong>Чистая вода</strong><small>${cleanWaterState}</small></span></div>\n          <div class="legend-row dirty-water"><ha-icon icon="mdi:water-opacity"></ha-icon><span class="legend-copy"><strong>Грязная вода</strong><small>${dirtyWaterState}</small></span></div>\n          <div class="legend-row dust ${dust ? "active" : ""}"><ha-icon icon="mdi:delete-outline"></ha-icon><span class="legend-copy"><strong>Пыль/мешок</strong><small>${dustState}</small></span></div>\n          <div class="legend-row dry ${dry ? "active" : ""}"><ha-icon icon="mdi:weather-windy"></ha-icon><span class="legend-copy"><strong>Тёплый воздух</strong><small>${dryState}</small></span></div>\n          <div class="legend-row charge ${chargeActive ? "active" : ""}"><ha-icon icon="${charging ? "mdi:battery-charging" : "mdi:flash"}"></ha-icon><span class="legend-copy"><strong>Зарядка</strong><small>${chargeState}</small></span></div>\n        </div>\n      </div>\n      <div class="hero-metrics">\n        <div data-more="battery"><ha-icon class="metric-icon battery" icon="mdi:battery-medium"></ha-icon><span>АКБ</span><strong>${battery}</strong><small>Текущий заряд</small><div class="battery-bar"><i style="width:${snap.battery ?? 0}%"></i></div></div>\n        <div data-more="mode"><ha-icon class="metric-icon mode" icon="mdi:fan"></ha-icon><span>Режим</span><strong>${escapeHtml(mode)}</strong><small>${mode === "Smart" ? "Автовыбор" : "Профиль уборки"}</small></div>\n        <div data-more="telemetry_age"><ha-icon class="metric-icon telemetry" icon="mdi:signal-cellular-3"></ha-icon><span>Телеметрия</span><strong>${escapeHtml(age)}</strong><small>Последнее обновление</small></div>\n      </div>\n    </section>`;\n  }\n'''
text = text[:hero_start] + new_hero + text[hero_end:]

quick_start = text.index('  _quickActions() {')
quick_end = text.index('\n  _overview() {', quick_start)
new_quick = '''  _quickActions() {\n    const snap = this._snapshot(); const vacuum = snap.vacuum;\n    const available = snap.connected && this._available(vacuum);\n    const cleaning = vacuum?.state === "cleaning"; const paused = vacuum?.state === "paused";\n    const docked = snap.onDock === true || ["charging", "charged"].includes(snap.robot);\n    const startClass = cleaning ? "action running" : docked ? "action ready" : "action primary";\n    const pauseClass = cleaning ? "action primary" : "action";\n    const homeClass = docked ? "action primary running" : "action";\n    const startTitle = cleaning ? "Уборка" : paused ? "Продолжить" : "Уборка";\n    return `<div class="quick-actions"><button class="${startClass}" data-action="start" ${available && !cleaning ? "" : "disabled"}><span class="action-icon"><ha-icon icon="${cleaning ? "mdi:robot-vacuum" : "mdi:play"}"></ha-icon></span><strong>${startTitle}</strong><span class="action-sub">${cleaning ? "Идёт" : paused ? "Возобновить" : "Smart"}</span></button><button class="${pauseClass}" data-action="pause" ${available && cleaning ? "" : "disabled"}><span class="action-icon"><ha-icon icon="mdi:pause"></ha-icon></span><strong>Пауза</strong><span class="action-sub">${cleaning ? "Приостановить" : "Недоступно"}</span></button><button class="${homeClass}" data-action="home" ${available && !docked ? "" : "disabled"}><span class="action-icon"><ha-icon icon="mdi:home"></ha-icon></span><strong>Домой</strong><span class="action-sub">${docked ? "На базе" : "На станцию"}</span></button></div>`;\n  }\n'''
text = text[:quick_start] + new_quick + text[quick_end:]

overview_start = text.index('  _overview() {')
overview_end = text.index('\n  _cleaning() {', overview_start)
new_overview = '''  _overview() {\n    const snap = this._snapshot();\n    const robot = snap.unreliable ? "Нет данных" : this._label(ROBOT_LABELS, snap.robot, "Нет данных");\n    const stationRaw = snap.unreliable ? "Нет данных" : this._label(STATION_LABELS, snap.station, "Нет данных");\n    const station = snap.station === "idle" && !snap.unreliable ? "Готова" : stationRaw;\n    const robotContext = snap.unreliable ? "Нет данных" : snap.onDock === true ? "На базе" : snap.onDock === false ? "В работе" : "Позиция неизвестна";\n    const operation = snap.unreliable ? "Нет данных" : snap.stationOperations.length ? snap.stationOperations.map((x) => STATION_OPERATION_LABELS[x] || x).join(" · ") : snap.missingStationDps.length ? "Часть данных" : "В ожидании";\n    return `<div>${this._hero()}${this._trustBanner(snap)}${this._quickActions()}<section class="card statuses-card"><div class="statuses-head"><h2>Статусы</h2><button type="button" data-view="station">Все <ha-icon icon="mdi:chevron-right"></ha-icon></button></div><div class="status-grid">\n      <button class="status-card good" data-more="robot_status" type="button"><span class="status-icon robot"><ha-icon icon="mdi:robot-vacuum"></ha-icon></span><strong>Робот</strong><b>${escapeHtml(robot)}</b><span class="meta">${escapeHtml(robotContext)}</span></button>\n      <button class="status-card good" data-more="station_status" type="button"><span class="status-icon station"><ha-icon icon="mdi:home-automation"></ha-icon></span><strong>Станция</strong><b>${escapeHtml(station)}</b><span class="meta">${escapeHtml(operation)}</span></button>\n      <div class="status-card neutral"><span class="status-icon water"><ha-icon icon="mdi:cup-water"></ha-icon></span><strong>Чистая вода</strong><b>—</b><span class="meta">Нет датчика уровня</span></div>\n      <div class="status-card neutral"><span class="status-icon dustbin"><ha-icon icon="mdi:delete-outline"></ha-icon></span><strong>Пылесборник</strong><b>—</b><span class="meta">Нет датчика заполнения</span></div>\n    </div></section></div>`;\n  }\n'''
text = text[:overview_start] + new_overview + text[overview_end:]

# Replace the visual contract blocks as a unit.
text, count = re.subn(
    r'      \.omni-scene\{.*?\n      \.trust-banner\{',
    '''      .omni-scene{position:relative;z-index:1;height:236px;margin-top:12px;border-radius:22px;border:1px solid color-mix(in srgb,var(--divider-color) 64%,transparent);background:linear-gradient(145deg,#ffffff 0%,#f7fbfd 56%,#edf8fc 100%);box-shadow:inset 0 1px 0 rgba(255,255,255,.98),0 8px 24px rgba(18,56,72,.035);overflow:hidden}.omni-art{position:absolute;left:0;top:0;bottom:0;width:75%;z-index:2;overflow:hidden;border-radius:21px}.product-art{display:block;width:100%;height:100%;object-fit:cover;object-position:center;border-radius:21px;transition:opacity .2s ease,filter .2s ease}.omni-art.muted .product-art{opacity:.48;filter:grayscale(.35)}.scene-local{position:absolute;left:12px;top:12px;display:inline-flex;align-items:center;gap:6px;min-height:30px;padding:0 10px;border-radius:999px;background:rgba(225,242,255,.94);color:#087cc0;font-size:11px;font-weight:800;box-shadow:0 4px 12px rgba(20,86,120,.08)}.scene-local ha-icon{--mdc-icon-size:17px}.omni-legend{position:absolute;right:8px;top:10px;bottom:10px;width:31%;z-index:5;display:flex;flex-direction:column;justify-content:center;gap:5px;padding:8px;border-radius:19px;background:rgba(255,255,255,.97);border:1px solid rgba(101,112,118,.11);box-shadow:0 10px 26px rgba(0,0,0,.075);backdrop-filter:blur(14px) saturate(115%)}.legend-row{